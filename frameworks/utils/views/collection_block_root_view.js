Endash.CollectionBlockRootView = Endash.CollectionBlockView.extend({
  
  // will be set by the collection view to whatever indexes the block view is going to show
  indexes: null,

  childrenBinding: '.content',
  
  // this is just a container
  _setMyView: function() {},
  
  
})